# Spec vs. Codebase Discrepancy Review Matrix

> **Target Document:** [`conductor/spec.md`](./spec.md)  
> **Last Audit:** 2026-08-27  
> **Generated By:** `/spec_drift` Skill  
> **Resolution Status:** **100% Fully Aligned & Synchronized (All 16 Discrepancies Resolved)**

---

## 🔍 Discrepancy Matrix & Final Resolution Status

| ID | Domain / Component | Specification Version | Implemented Codebase Version | Resolution | Status |
|:---|:---|:---|:---|:---|:---:|
| **D-1** | **`config.json` Schema** | Documents 9 feature toggles including `"ledger"`. | Contains 9 feature toggles matching spec. | **Spec Aligned** | ✅ Resolved |
| **D-2** | **`risks.json` Field Naming** | Commonwealth `camelCase` domain fields aligned with Defence risk standards. | Commonwealth `camelCase` domain fields (`inherentLikelihood`, `treatmentPlan`, etc.). | **Spec Aligned** | ✅ Resolved |
| **D-3** | **`issues.json` Schema** | Program issue register schema (`I-0001`, `displayId`, `priority`, `status`, `actionPlan`). | Fully aligned with `issues.json`. | **Spec Aligned** | ✅ Resolved |
| **D-4** | **`driver_tree.json` Schema** | Root object is `{"capabilityDrops": [...]}` without synthetic metadata. | Matches production and sample schema cleanly. | **Spec Aligned** | ✅ Resolved |
| **D-5** | **`snapshots.json` Schema** | Includes `plans: [...]` array and complete `sleeperOutlier` properties. | Fully aligned with snapshot parser output. | **Spec Aligned** | ✅ Resolved |
| **D-6** | **Synthesis JSON Schemas** | Flat snapshot properties (`synthesis`, `top3`, `sleeperOutlier`, `podcastScript`). | Matches Pydantic schemas in `scripts/gemini_generator.py`. | **Spec Aligned** | ✅ Resolved |
| **D-7** | **`server.py` REST Endpoints** | Section 2.2 and Section 5.1 aligned to 4 rationalized resources with alias notes. | `server.py` implements 4 rationalized handlers with backward-compatible aliases. | **Spec Aligned** | ✅ Resolved |
| **D-8** | **Tab 1 KPI Pillars** | 4 program pillars (Commercial, Milestone 2 / IBR, ATO & Security, Critical Escalations). | 4 program pillars matching `config.json`. | **Spec Aligned** | ✅ Resolved |
| **D-9** | **Audio Podcast Data Format** | `podcastScript` multi-turn dialogue array with timestamps and avatar badges. | Matches multi-turn schema and `generate_multispeaker_podcast`. | **Spec Aligned** | ✅ Resolved |
| **D-10** | **Tab Index & Ordering** | FR-4 = Issues (Tab 3), FR-5 = Team Google Risks (Tab 4). | Matches `index.html` navigation bar order. | **Spec Aligned** | ✅ Resolved |
| **D-11** | **Tab 8 (Master Ledger)** | FR-9: Whole Register Master Ledger added to Section 4. | Implemented via `tab-ledger` and `features.ledger`. | **Spec Aligned** | ✅ Resolved |
| **D-12** | **AI Model Branding** | Cites "Gemini" / "Gemini 3.5 Flash" (Bug #58 invariant). | Matches `gemini-3.5-flash` in generator and UI copy. | **Spec Aligned** | ✅ Resolved |
| **D-13** | **`knowledge.json` Schema** | Section 3.1.6 explicitly defines blueprint schema. | Matches `data/sample/knowledge.json`. | **Spec Aligned** | ✅ Resolved |
| **D-14** | **Automated Test Count** | All sections aligned to **127 automated unit tests**. | Codebase executes **127 passing unit tests (100%)**. | **Spec Aligned** | ✅ Resolved |
| **D-15** | **Directory & Archival Layout** | Section 3.2 tree documents `archive/` subfolders and clean `src/js/` structure. | Matches codebase directory layout. | **Spec Aligned** | ✅ Resolved |
| **D-16** | **Consolidated Pipeline** | Section 2.2 & 5.2 cite `scripts/pipeline.py` as unified CLI and engine. | Ingestion consolidated into `scripts/pipeline.py`. | **Spec Aligned** | ✅ Resolved |
