# Spec vs. Codebase Discrepancy Review Matrix

> **Target Document:** [`conductor/spec.md`](./spec.md)  
> **Last Audit:** 2026-08-19  
> **Generated By:** `/spec_drift` Skill

---

## 🔍 Active Discrepancy Matrix

The following table tracks known discrepancies between the Master Specification draft and the codebase. Review each item to decide whether to adopt the **Spec version** (modifying code), the **Code version** (updating the spec), or an **Alternative**:

| ID | Domain / Component | Draft Spec (`spec_draft.md`) Version | Implemented Codebase Version | Recommendation | Resolution Status | User Decision / Action |
|:---|:---|:---|:---|:---|:---:|:---|
| **D-1** | **`config.json` Schema** | Uses basic `"theme": { "primaryColor": "...", "primaryHex": "..." }` and simple `"sources"` URLs. | Uses rich `"kpiPillars": [...]`, `"sources": { ... "enabled": false }`, and full `"features": { ... }` toggle matrix. | **Adopt Code** (Update Spec to document `kpiPillars` & feature toggles) | ✅ Resolved | **Spec Updated**: Added `kpiPillars` and `features` matrix to `spec_draft.md`. |
| **D-2** | **`risks.json` Field Naming** | Uses generic `snake_case`: `inherent_likelihood`, `inherent_impact`, `residual_likelihood`, `owner`, `mitigation`, `is_team_google`. | Uses Commonwealth `camelCase`: `inherentLikelihood`, `inherentConsequence`, `inherentRiskScore`, `residualLikelihood`, `residualConsequence`, `riskOwner`, `treatmentOwner`, `treatmentPlan`, `causeCategory`, `bundle`, `driverTreeRef`, `sourceRegister` (`"joint"` vs `"google"`). | **Adopt Code** (Update Spec to match Commonwealth risk standards & calculation engine) | ✅ Resolved | **Spec Updated**: Replaced snake_case with Commonwealth camelCase domain fields. |
| **D-3** | **`issues.json` Schema & Tiers** | Uses `id: "ISS-001"`, `priority: "P1"`, `impact: "High"`, `status: "Open"`, `resolution_plan`. | Uses `id: "AUR-ISS-001"` (or `"I-0001"` in F-DSE), `displayId: "ISS-01"`, `priority: "Urgent" \| "High"`, `severity: "Critical"`, `status: "Active" \| "Closed"`, `actionPlan`, `impactCategory`, `driverTreeRef`, `bundle`, `escalateTo`. | **Adopt Code** (Matches raw source registers) | ✅ Resolved | **Spec Updated**: Documented source issue schema (`I-0001`, `displayId`, `actionPlan`, `driverTreeRef`). |
| **D-4** | **`driver_tree.json` & Linking** | Structured as `{ "milestones": [ { "deliverables": [ { "related_risks": [...] } ] } ] }` with hardcoded risk arrays. | Structured as `{ "capabilityDrops": [ { "gates": [ { "ref": "1.2b", "level": 2, ... } ] } ] }`. Badges are dynamic: `risks.json` & `issues.json` declare `driverTreeRef`, and the UI matches them at runtime. | **Adopt Code** (Matches raw capability drops & avoids brittle hardcoding) | ✅ Resolved | **Spec Updated**: Documented `capabilityDrops` -> `gates` and dynamic `driverTreeRef` binding. |
| **D-5** | **`snapshots.json` Root Structure** | Described as a flat JSON Array `[ { "week": "W27", "metrics": {...}, "synthesis": {...} } ]`. | Structured as a JSON Map object `{ "lastSynced": "...", "driveFolderId": "...", "snapshots": { "w22": {...}, "w27": {...} } }`. | **Adopt Code** (Update Spec to document week-keyed snapshot map) | ✅ Resolved | **Spec Updated**: Documented week-keyed map structure in `spec_draft.md`. |
| **D-6** | **Synthesis & Action Items JSON** | Nested structure placing `top_actions` and `sleeper_outlier` inside `synthesis.executive`. | Flat top-level snapshot properties: `synthesis: { executive, technical, governance }` (60-80 word paragraphs), `top3: [...]`, `sleeperOutlier: {...}`, `podcastScript: [...]`. Matches Pydantic validation schema. | **Adopt Code** (Update Spec to match Pydantic schema in `gemini_generator.py`) | ✅ Resolved | **Spec Updated**: Documented flat schema matching Pydantic validator in `spec_draft.md`. |
| **D-7** | **`server.py` REST API Endpoints** | Lists only 4 basic endpoints: `GET /`, `POST /api/sync-sheet`, `POST /api/ingest-data`, `POST /api/regenerate-briefing`. | Implements 10 endpoints: plus `GET /api/sync-all`, `POST /api/sync-all`, `GET /api/check-drive-sync`, `GET /api/notebooks`, `GET /api/check-notebook-sync`, `POST /api/sync-notebook`, and `GET / POST /api/ingest-report`. | **Review & Rationalise** | 🟡 Bug Raised | **Tracked in Bug #79**: Review and rationalise server endpoints. |
| **D-8** | **Tab 1 Top 4 KPI Banner** | Lists: (1) Critical Risks, (2) Active Issues, (3) Schedule Squeeze Barometer, (4) Security ATO Gate. | Top 4 Header KPIs: (1) Commercial & Budget, (2) Milestone Gate (IBR), (3) ATO-C Gate, (4) Critical Escalations. (5×5 Risk score, issues, and squeeze appear in sub-panels). | **Adopt Code** (Update Spec to reflect 4 program pillars + sub-panel tiles) | ✅ Resolved | **Spec Updated**: Updated FR-2.1 to reflect 4 program pillars and sub-panels. |
| **D-9** | **Audio Podcast Data Format** | Assumes static MP3 file path `audio_briefing.audio_file`. | Stores multi-turn conversational script in `podcastScript: [ { speaker, role, avatar, time, text } ]` supporting interactive transcript drawer with timestamps and Gemini TTS audio. | **Adopt Code** (Update Spec to document `podcastScript` turn schema) | ✅ Resolved | **Spec Updated**: Documented `podcastScript` turn schema in `spec_draft.md`. |

---

## 📋 Detailed Discrepancy Breakdown & Source Data Findings

### D-1: `config.json` Schema (`✅ Resolved`)
- **Resolved**: Updated `conductor/spec_draft.md` to document the full `kpiPillars`, feature toggles (`features`), and multi-stream source configuration.

### D-2: `risks.json` Field Naming & Scoring (`✅ Resolved`)
- **Resolved**: Updated `conductor/spec_draft.md` to align with Commonwealth standard camelCase (`inherentLikelihood`, `inherentConsequence`, `inherentRiskScore`, `residualLikelihood`, `residualConsequence`, `treatmentPlan`, `driverTreeRef`, `sourceRegister`).

### D-3: `issues.json` Source Data Analysis (`✅ Resolved`)
- **Resolved**: Updated `conductor/spec_draft.md` to document the authentic program issue register schema (`I-0001`, `displayId`, `priority: "Urgent"`, `status: "Active"`, `actionPlan`, `impactCategory`, and `driverTreeRef`).

### D-4: `driver_tree.json` Source Data Analysis (`✅ Resolved`)
- **Resolved**: Updated `conductor/spec_draft.md` to document the contractual delivery hierarchy (`capabilityDrops` $\rightarrow$ `gates`) and dynamic `driverTreeRef` cross-linking (eliminating brittle static risk arrays).

### D-5: `snapshots.json` Root Structure (`✅ Resolved`)
- **Resolved**: Updated `conductor/spec_draft.md` to document the week-keyed map format `{ "lastSynced": "...", "snapshots": { "w22": {...}, "w27": {...} } }`.

### D-6: AI Synthesis & Action Items JSON Schema (`✅ Resolved`)
- **Resolved**: Updated `conductor/spec_draft.md` to document flat top-level snapshot fields (`synthesis`, `top3`, `sleeperOutlier`, `podcastScript`) matching Pydantic validation schemas in `scripts/gemini_generator.py`.

### D-7: `server.py` REST API Endpoints (`🟡 Bug #79 Raised`)
- **Bug Raised**: Created **Bug #79** in `.agents/bugs.json` (*"Review and rationalise server.py REST API endpoints"*).
- The current 10 endpoints remain active in `server.py` and are documented in the specification until Bug #79 is triaged and resolved.

### D-8: Tab 1 Top 4 KPI Banner (`✅ Resolved`)
- **Resolved**: Updated FR-2.1 in `conductor/spec_draft.md` to reflect the 4 program pillars (Commercial & Budget, Milestone Gate / IBR, ATO & Security, Critical Escalations) and noted the sub-panel risk/issue/squeeze metrics.

### D-9: Audio Podcast Data Format (`✅ Resolved`)
- **Resolved**: Updated `conductor/spec_draft.md` to document the `podcastScript` turn schema with timestamps and avatars.
